<h1>New Order</h1>

<%= form_with model: @order do |form| %>
  <%= form.hidden_field :product_id, value: @product.id if @product %>
  <%= form.hidden_field :user_id, value: @user.id if @user %>
  <!-- <div>
    <%= form.label :user_id %>
    <%= form.number_field :user_id %> 
  </div>   -->
  
  <div>
    <%= form.submit "Create Order" %>
  </div>
<% end %>
